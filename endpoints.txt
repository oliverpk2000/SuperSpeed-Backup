list of necessary endpoints:
    get/
    get/:id
    post/
    update/:id
    delete/:id
each of these endpoints for: category, game, runner and speedrun

Base URL (for now): localhost:7000/superspeed/{object}